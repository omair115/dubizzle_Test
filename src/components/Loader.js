import React from 'react';
import './loader.css'; // You can create a CSS file for styling

const FullScreenLoader = () => {
  return (
    <div className="fullscreen-loader">
      <div className="loader"></div>
    </div>
  );
};

export default FullScreenLoader;